
import { StyleSheet } from "react-native"
export const colores = {
    primary: '#048ABF'
}

export const styles = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 50
    },
    title: {
        fontSize: 30,
        marginBottom: 20
    }
})
